apiVersion: training.eduk8s.io/v1alpha1
kind: Workshop
metadata:
  name: lab-docker-testing
spec:
  vendor: eduk8s.io
  title: Markdown Sample
  description: A sample workshop using Markdown
  url: https://github.com/eduk8s/lab-docker-testing
  content: github.com/eduk8s-tests/lab-docker-testing
  duration: 60m
  session:
    budget: medium
    applications:
      terminal:
        enabled: true
        layout: split
      console:
        enabled: true
    env:
    - name: REGISTRY_HOST
      value: $(session_namespace)-registry.$(ingress_domain)
    - name: REGISTRY_USERNAME
      value: eduk8s
    - name: REGISTRY_PASSWORD
      value: eduk8s
    patches:
      containers:
      - name: workshop
        env:
        - name: DOCKER_CERT_PATH
          value: /certs/client
        - name: DOCKER_HOST
          value: tcp://127.0.0.1:2376/
        - name: DOCKER_TLS_VERIFY
          value: "1"
        volumeMounts:
        - name: registry
          mountPath: /var/run/registry
        - name: docker-certs
          mountPath: /certs/client
          subPath: client
          readOnly: true
      - name: docker
        image: docker:dind
        securityContext:
          privileged: true
        requests:
          memory: 256Mi
        limits:
          memory: 768Mi
        env:
        - name: DOCKER_TLS_CERTDIR
          value: /certs
        volumeMounts:
        - name: docker-certs
          mountPath: /certs
        - name: docker
          mountPath: /var/lib/docker
      volumes:
      - name: docker
        persistentVolumeClaim:
          claimName: $(session_namespace)-docker
      - name: registry
        configMap:
          name: $(session_namespace)-registry
          items:
          - key: config.json
            path: config.json
      - name: docker-certs
        emptyDir: {}
      - name: docker-user
        emptyDir: {}
    objects:
    - apiVersion: v1
      kind: PersistentVolumeClaim
      metadata:
        namespace: $(workshop_namespace)
        name: $(session_namespace)-docker
      spec:
        accessModes:
        - ReadWriteOnce
        resources:
          requests:
            storage: 5Gi
    - apiVersion: v1
      kind: PersistentVolumeClaim
      metadata:
        namespace: $(workshop_namespace)
        name: $(session_namespace)-registry
      spec:
        accessModes:
        - ReadWriteOnce
        resources:
          requests:
            storage: 5Gi
    - apiVersion: v1
      kind: ConfigMap
      metadata:
        namespace: $(workshop_namespace)
        name: $(session_namespace)-registry
      data:
        htpasswd: |-
          eduk8s:$2y$05$Oe5Khp7HbllL1oANqmCPSOy1O656z9Z6ETv6GI/DblawxnT/qVUjK
        config.json: |-
          {
            "auths": {
               "$(session_namespace)-registry.$(ingress_domain)": {
                 "auth": "ZWR1azhzOmVkdWs4cw=="
               }
            }
          }
    - apiVersion: apps/v1
      kind: Deployment
      metadata:
        namespace: $(workshop_namespace)
        name: $(session_namespace)-registry
      spec:
        replicas: 1
        selector:
          matchLabels:
            deployment: $(session_namespace)-registry
        strategy:
          type: Recreate
        template:
          metadata:
            labels:
              deployment: $(session_namespace)-registry
          spec:
            containers:
            - name: registry
              image: registry.hub.docker.com/library/registry:2.6.1
              imagePullPolicy: IfNotPresent
              ports:
              - containerPort: 5000
                protocol: TCP
              env:
              - name: REGISTRY_STORAGE_DELETE_ENABLED
                value: "true"
              - name: REGISTRY_AUTH
                value: htpasswd
              - name: REGISTRY_AUTH_HTPASSWD_REALM
                value: Image Registry
              - name: REGISTRY_AUTH_HTPASSWD_PATH
                value: /auth/htpasswd
              volumeMounts:
              - name: data
                mountPath: /var/lib/registry
              - name: auth
                mountPath: /auth
            securityContext:
              runAsUser: 1000
            volumes:
            - name: data
              persistentVolumeClaim:
                claimName: $(session_namespace)-registry
            - name: auth
              configMap:
                name: $(session_namespace)-registry
                items:
                - key: htpasswd
                  path: htpasswd
    - apiVersion: v1
      kind: Service
      metadata:
        namespace: $(workshop_namespace)
        name: $(session_namespace)-registry
      spec:
        type: ClusterIP
        ports:
        - port: 5000
          targetPort: 5000
        selector:
          deployment: $(session_namespace)-registry
    - apiVersion: extensions/v1beta1
      kind: Ingress
      metadata:
        namespace: $(workshop_namespace)
        name: $(session_namespace)-registry
        annotations:
          nginx.ingress.kubernetes.io/proxy-body-size: 512m
      spec:
        rules:
        - host: $(session_namespace)-registry.$(ingress_domain)
          http:
            paths:
            - path: "/"
              backend:
                serviceName: $(session_namespace)-registry
                servicePort: 5000
        tls:
        - hosts:
          - "*.$(ingress_domain)"
          secretName: "$(ingress_secret)"
    - apiVersion: rbac.authorization.k8s.io/v1
      kind: RoleBinding
      metadata:
        namespace: $(workshop_namespace)
        name: $(session_namespace)-docker
      roleRef:
        apiGroup: rbac.authorization.k8s.io
        kind: ClusterRole
        name: $(workshop_namespace)-docker
      subjects:
      - kind: ServiceAccount
        namespace: $(workshop_namespace)
        name: $(service_account)
  workshop:
    objects:
    - apiVersion: policy/v1beta1
      kind: PodSecurityPolicy
      metadata:
        name: $(workshop_namespace)-docker
      spec:
        privileged: true
        allowPrivilegeEscalation: true
        requiredDropCapabilities:
        - KILL
        - MKNOD
        - SETUID
        - SETGID
        hostIPC: false
        hostNetwork: false
        hostPID: false
        hostPorts: []
        runAsUser:
          rule: RunAsAny
        seLinux:
          rule: RunAsAny
        fsGroup:
          rule: RunAsAny
        supplementalGroups:
          rule: RunAsAny
        volumes:
        - configMap
        - downwardAPI
        - emptyDir
        - persistentVolumeClaim
        - projected
        - secret
    - apiVersion: rbac.authorization.k8s.io/v1
      kind: ClusterRole
      metadata:
        name: $(workshop_namespace)-docker
      rules:
      - apiGroups:
        - policy
        resources:
        - podsecuritypolicies
        verbs:
        - use
        resourceNames:
        - $(workshop_namespace)-docker
